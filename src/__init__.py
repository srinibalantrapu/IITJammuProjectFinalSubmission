"""Travel planner application package (CrewAI agents, pipeline, schemas).

Run with ``PYTHONPATH=src`` (or project root with ``python -m``) so modules such as
``agents`` and ``schemas`` resolve as today; this file marks ``src`` as a package
for tooling and future ``from src.…`` imports if you standardize on project-root path.
"""
